version: 1

providers:
  - node

phases:
  setup:
    nixPkgs:
      - nodejs_20  # Upgrade to Node 20
  install:
    cmds:
      - npm install
  build:
    cmds:
      - echo "✅ Bot with Node 20 and crypto fix ready"